[workspace]
members = ["host"]
exclude = ["examples/game_of_life"]  # Built separately with wasm32-wasip1 target
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
